import 'package:flutter/material.dart';

class RbcColors {
  RbcColors._();

  // Keep the full app inside a professional 3-color palette.
  static const Color primary = Color(0xFF102A43);
  static const Color accent = Color(0xFFF4B400);
  static const Color surface = Color(0xFFF7F3EA);
}
